// ⚠️ Automatically synced with ../gradle/libs.versions.toml
// Due to file sync, the first build updates this file.
// Run a build once to sync the versions; the updated version will be used on the next build.
CURRENT_PROJECT_VERSION=10000
MARKETING_VERSION=1.0.0


PRODUCT_BUNDLE_IDENTIFIER=com.kmpstarter.nativeapp
